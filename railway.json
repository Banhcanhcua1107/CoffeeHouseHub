{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "install": {
          "cmds": ["npm install --workspaces --include-workspace-root"]
        },
        "build": {
          "cmds": []
        },
        "start": {
          "cmds": ["npm start --workspace backend"]
        }
      }
    }
  }
}
